import { FOUNDRY_EIGHT, STARTER_STATE, preCacContribution, postCacContribution, maxFirstOrderCac, grossMarginPct, contributionMarginPct, breakEvenOrders, inventoryMath, wholesaleContribution, retailerGrossMarginPct, normalized3pl, crossoverOrders, cashCheckpoint, processorFee, realizedPrice, purchaseOrderGate, cacFromSpend, funnelMetrics } from './math.js';
import { summarizeShopifyCsv } from './shopify.js';

const VERSION='5.5'; const clone=x=>JSON.parse(JSON.stringify(x));
function safeStoredJson(key,fallback){
  try{
    const raw=localStorage.getItem(key);
    if(!raw)return fallback;
    const parsed=JSON.parse(raw);
    return parsed ?? fallback;
  }catch{return fallback}
}
const storedState=safeStoredJson('msbo_state',{});
let state={...clone(STARTER_STATE),...(storedState&&typeof storedState==='object'?storedState:{})};
let mode=localStorage.getItem('msbo_mode')||'fresh';
const storedTouched=safeStoredJson('msbo_touched',[]);
let touched=new Set(Array.isArray(storedTouched)?storedTouched:[]);
let lastImport=safeStoredJson('msbo_last_import',null);
localStorage.setItem('msbo_version',VERSION);
let config={authConfigured:false,cloudConfigured:false,billingConfigured:false},session=loadSession(),entitlement={plan:'free',active:false,billingManageable:false,limits:{brands:1,skus:5},usage:{brands:0,skus:0}};
let current='dashboard',brands=[],skus=[];
const $=s=>document.querySelector(s); const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number.isFinite(+n)?+n:0); const pct=n=>`${(+n||0).toFixed(1)}%`;
const save=()=>{localStorage.setItem('msbo_state',JSON.stringify(state));localStorage.setItem('msbo_mode',mode);localStorage.setItem('msbo_version',VERSION);localStorage.setItem('msbo_touched',JSON.stringify([...touched]));if(lastImport)localStorage.setItem('msbo_last_import',JSON.stringify(lastImport));else localStorage.removeItem('msbo_last_import')};
const status=(kind,label)=>`<span class="status ${kind}">${label}</span>`;
const metric=(a,b,c='',kind='',info=null)=>`<div class="metric"><span class="metricAccent"></span><div class="metricTop"><small>${a}</small>${kind?status(kind,kind==='good'?'HEALTHY':kind==='warn'?'WATCH':'FIX'):''}</div><strong>${b}</strong><div class="metricNote">${c}</div>${info?`<details class="metricDetails"><summary>What does this mean?</summary><div class="mini mt">${info.definition?`<p><b>What it is:</b> ${info.definition}</p>`:''}${info.why?`<p><b>Why it matters:</b> ${info.why}</p>`:''}${info.math?`<p><b>How it is calculated:</b> ${info.math}</p>`:''}${info.action?`<p><b>What to do next:</b> ${info.action}</p>`:''}${info.source?`<p><b>Based on:</b> ${info.source}</p>`:''}</div></details>`:''}</div>`;
const hasInput=key=>mode==='demo'||mode==='demo-edited'||touched.has(key);
const field=(key,label,help='',step='.01',placeholder='Enter value')=>`<div class="field"><label>${label}</label><input data-key="${key}" type="number" step="${step}" value="${hasInput(key)?state[key]:''}" placeholder="${placeholder}">${help?`<small>${help}</small>`:''}</div>`;
const sourceBadge=(label=mode==='demo'?'DEMO CASE':mode==='demo-edited'?'EDITED DEMO':'USER INPUT')=>`<span class="sourceTag">${label}</span>`;
const row=(a,b)=>`<div class="row"><span>${a}</span><b>${b}</b></div>`;
const head=(t,d,s)=>`<div class="toolhead"><div><span class="kicker">OPERATING TOOL</span><h2>${t}</h2><p class="muted">${d}</p></div><span class="source">${s}</span></div>`;
const readyProduct=()=>hasInput('price')&&state.price>0&&hasInput('landedCost')&&state.landedCost>=0;
const readyCacFloor=()=>readyProduct()&&hasInput('requiredPostCac');
const readyCac=()=>readyCacFloor()&&(hasInput('observedCac')||(hasInput('adSpend')&&hasInput('newCustomers')&&state.newCustomers>0));
const readyInv=()=>hasInput('weeklyDemand')&&state.weeklyDemand>0&&hasInput('leadWeeks')&&state.leadWeeks>0&&hasInput('onHand');
const readyCash=()=>hasInput('cashStart')&&hasInput('protectedFloor');
const calculatedCac=()=>hasInput('adSpend')&&hasInput('newCustomers')&&state.newCustomers>0?cacFromSpend(state):null;
const effectiveCac=()=>hasInput('observedCac')?state.observedCac:calculatedCac();
const effectiveState=()=>({...state,observedCac:effectiveCac()??0});
const readyFunnel=()=>hasInput('sessions')&&state.sessions>0&&hasInput('orders');
const missingOrderCosts=()=>['packaging','shippingSubsidy','returnReserve','processorPercent','processorFixed'].filter(k=>!hasInput(k));
function loadSession(){try{return JSON.parse(localStorage.getItem('msbo_session')||'null')}catch{return null}}
function storeSession(s){session=s||null;if(session)localStorage.setItem('msbo_session',JSON.stringify(session));else localStorage.removeItem('msbo_session')}
async function api(url,method='GET',body,withAuth=false){
  const headers={'Content-Type':'application/json'};if(withAuth&&session?.access_token)headers.Authorization=`Bearer ${session.access_token}`;
  let r;
  try{r=await fetch(url,{method,headers,body:body?JSON.stringify(body):undefined})}catch{throw new Error('Brand OS could not reach its server. Refresh the page and check the deployment status.')}
  if(r.status===401&&withAuth&&session?.refresh_token){
    let rr;try{rr=await fetch('/api/auth/refresh',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({refresh_token:session.refresh_token})})}catch{throw new Error('Could not refresh your session.')}
    if(rr.ok){const j=await rr.json();storeSession(j.session);headers.Authorization=`Bearer ${session.access_token}`;r=await fetch(url,{method,headers,body:body?JSON.stringify(body):undefined})}
  }
  const j=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(j.error||`Request failed (${r.status})`);e.status=r.status;e.code=j.code||null;throw e}return j
}
async function init(){config=await fetch('/api/public-config').then(r=>r.json()).catch(()=>config);await refreshAccount();track('landing_page_viewed');render()}
async function refreshAccount(){if(!session){entitlement={plan:config.authConfigured?'free':'local',active:false,billingManageable:false,limits:{brands:1,skus:5},usage:{brands:0,skus:0}};brands=[];skus=[];updateAccount();return}let acct;try{acct=await api('/api/account','GET',null,true)}catch(e){if(e.status===401){storeSession(null);brands=[];skus=[];entitlement={plan:'free',active:false,billingManageable:false,limits:{brands:1,skus:5},usage:{brands:0,skus:0}}}else console.error('refresh-account',e);updateAccount();return}entitlement={plan:acct.plan,active:acct.active,billingManageable:Boolean(acct.billingManageable),limits:acct.limits||{brands:1,skus:5},usage:acct.usage||{brands:0,skus:0}};try{await loadCloud()}catch(e){console.error('load-cloud',e)}updateAccount()}
function updateAccount(){if(!$('#planPill'))return;$('#planPill').textContent=session?entitlement.plan.toUpperCase():(mode==='demo'?'DEMO':mode==='demo-edited'?'DEMO*':'LOCAL');$('#authBtn').textContent=session?'Account':'Sign in';$('#billingBtn').classList.toggle('hidden',!session||!config.billingConfigured);$('#billingBtn').textContent=(entitlement.active||entitlement.billingManageable)?'Billing':'Upgrade'}
async function loadCloud(){const [b,s]=await Promise.all([api('/api/brands','GET',null,true),api('/api/skus','GET',null,true)]);brands=b.brands||[];skus=s.skus||[]}

const analyticsId=localStorage.getItem('msbo_anon_id')||crypto.randomUUID();localStorage.setItem('msbo_anon_id',analyticsId);
const tracked=safeStoredJson('msbo_tracked_events',{});
async function track(name,properties={}){try{await fetch('/api/events',{method:'POST',headers:{'Content-Type':'application/json',...(session?.access_token?{Authorization:`Bearer ${session.access_token}`}:{})},body:JSON.stringify({event_name:name,anonymous_id:analyticsId,properties})})}catch{}}
function trackOnce(name,properties={}){if(tracked[name])return;tracked[name]=true;localStorage.setItem('msbo_tracked_events',JSON.stringify(tracked));track(name,properties)}

const views={dashboard:'Business Health',profit:'Profit & Pricing',cac:'Customer Acquisition Cost',funnel:'Store Funnel',discount:'Discount Ceiling',inventory:'Inventory & Reorder',wholesale:'Wholesale Economics',launch:'Launch & Break-Even',fulfillment:'3PL Decision',cash:'Cash Checkpoint',po:'PO Cash Gate',shopify:'Shopify CSV Dashboard',brands:'Brands & SKUs',advisor:'Next Move Advisor'};
const PRO_VIEWS=new Set(['discount','inventory','wholesale','launch','fulfillment','cash','po','shopify']);
const isPro=()=>Boolean(entitlement?.active);
const isProLocked=view=>Boolean(config.billingConfigured&&!isPro()&&mode!=='demo'&&mode!=='demo-edited'&&PRO_VIEWS.has(view));
function proGate(view){
  const reasons={discount:['Discount Ceiling','Know how deep you can discount without crossing the contribution floor you set.'],inventory:['Inventory & Reorder','Turn demand and supplier lead time into a reorder review trigger instead of guessing.'],wholesale:['Wholesale Economics','Check whether a wholesale deal leaves enough unit contribution before you accept it.'],launch:['Launch & Break-Even','See how many contribution-positive orders are required to recover fixed launch costs.'],fulfillment:['3PL Decision','Normalize fulfillment costs so monthly minimums do not hide the real per-order cost.'],cash:['Cash Checkpoint','Stress-test near-term liquidity against the cash floor you choose to protect.'],po:['PO Cash Gate','Test an inventory commitment against protected operating cash before approving the PO.'],shopify:['Shopify Data','Bring store data into Brand OS without pretending an export is the same as accounting profit.']};
  const [title,why]=reasons[view]||['Brand OS Pro','Unlock the operating tools that turn your model into an ongoing decision system.'];
  return `${head(title,'This is part of the recurring operating layer in Brand OS Pro.','PRO')}<div class="card"><span class="kicker">WHY YOU NEED THIS</span><h3>${title}</h3><p>${why}</p><div class="advice mt"><b>Free shows you the core economics.</b> Pro adds the tools designed for repeated operating decisions: inventory, cash, offers, fulfillment, wholesale and store-data workflows.</div><button class="primary mt" id="upgradeNow">${session?'Upgrade to Pro':'Create an account to upgrade'}</button></div>`
}
async function upgradeFlow(){if(!session){showAuth();return}await startBilling()}

function onboarding(){
  const core=[['Product economics',readyProduct(),'Add selling price + landed cost so Brand OS can model one real order.','profit'],['Acquisition guardrail',readyCacFloor(),'Choose the contribution you want left after acquiring a customer.','cac'],['Store funnel',readyFunnel(),'Add one consistent date range so drop-off can be measured without guessing at the cause.','funnel']];
  const operating=[['Inventory timing',readyInv(),'Use demand + supplier lead time to know when a reorder deserves review.','inventory'],['Cash protection',readyCash(),'Choose the operating cash floor you do not want a PO or expense to cross.','cash']];
  const items=isPro()||mode==='demo'||mode==='demo-edited'?[...core,...operating]:core;const done=items.filter(x=>x[1]).length;const payoff=done===items.length?'Your core model is ready. Brand OS can now interpret the numbers you supplied.':'Complete the steps below so Brand OS can explain what your numbers mean instead of showing empty KPIs.';
  return `<div class="onboard"><div><span class="kicker">SETUP ${done}/${items.length}</span><h2>Build the model Brand OS will use to guide decisions.</h2><p>${payoff}</p><div class="sourceLine">${sourceBadge()}</div></div><div class="setupList">${items.map(x=>`<button data-jump="${x[3]}" class="setupItem ${x[1]?'done':''}"><span>${x[1]?'✓':'○'}</span><div><b>${x[0]}</b><small>${x[2]}</small></div></button>`).join('')}${!isPro()&&config.billingConfigured?`<div class="setupItem"><span>PRO</span><div><b>Full operating model</b><small>Pro adds inventory, cash, PO, discount, wholesale, 3PL and store-data decision tools after the core setup.</small></div></div>`:''}</div></div>`
}

function dashboard(){if(!readyProduct())return `${onboarding()}<div class="sectionbar"><h3>Start with decisions that cost money</h3><span>No fake KPIs</span></div><div class="healthgrid"><div class="healthcard"><b>Profit before revenue</b><p>Build one product's real order economics before you spend to acquire customers.</p></div><div class="healthcard"><b>Cash before inventory</b><p>Protect operating cash before approving a production order.</p></div><div class="healthcard"><b>Demand before reorder</b><p>Use actual sales pace and lead time; comments and waitlists are signals, not guaranteed demand.</p></div></div>`;const es=effectiveState(),c=preCacContribution(es),pc=readyCac()?postCacContribution(es):null,im=inventoryMath(state),maxcac=readyCacFloor()?maxFirstOrderCac(es):null,be=readyCac()&&hasInput('fixedLaunchCost')?breakEvenOrders(es):Infinity,cashx=readyCash()?cashCheckpoint(state):null,poc=readyCash()&&hasInput('proposedUnits')&&hasInput('depositPct')?purchaseOrderGate(state):null;const profitKind=readyCac()?(pc>=state.requiredPostCac?'good':pc>0?'warn':'bad'):'warn',cacKind=readyCac()?(effectiveCac()<=maxcac?'good':effectiveCac()<=maxcac*1.15?'warn':'bad'):'warn',invKind=readyInv()?(im.inventoryPosition>im.reorderPoint?'good':'warn'):'warn',cashKind=readyCash()?(cashx.headroom>=0?'good':'bad'):'warn',fullOperating=isPro()||mode==='demo'||mode==='demo-edited',funnelx=funnelMetrics(state),next=nextMoves()[0]||['Finish setup','Enter enough real inputs for Brand OS to make a decision.'];const importBlock=fullOperating&&lastImport?`<div class="sectionbar"><h3>Store pulse</h3><span>${sourceBadge('SHOPIFY IMPORT')}</span></div>${renderImport(lastImport,true)}`:'';return `${onboarding()}<div class="dashboardIntro"><div><span class="kicker">BUSINESS HEALTH</span><h2>See only the numbers you actually supplied.</h2><p>${mode==='demo'?'<b>Demo mode:</b> every number below is the Foundry Eight case, not your brand.':mode==='demo-edited'?'<b>Edited demo:</b> this model still contains Foundry Eight case inputs unless you replace them.':'Blank inputs stay blank. No benchmark or business result is invented for you.'}</p></div><div class="brandchip"><small>Current model</small><b>${mode==='demo'?'Foundry Eight':mode==='demo-edited'?'Edited demo':'Your inputs'}</b></div></div><div class="grid g4">${metric('Contribution / order',money(c),'Before CAC',profitKind)}${metric('Post-CAC contribution',readyCac()?money(pc):'—',readyCac()?'After acquisition':'Add CAC inputs',profitKind)}${metric('Max first-order CAC',readyCacFloor()?money(maxcac):'—','From your contribution floor',cacKind)}${metric('Store conversion',readyFunnel()?pct(funnelx.conversionRate):'—',readyFunnel()?'Orders ÷ sessions':'Add funnel inputs')}</div>${fullOperating?`<div class="grid g4 mt">${metric('Inventory position',readyInv()?Math.ceil(im.inventoryPosition):'—',readyInv()?'On hand + inbound − allocated':'Add inventory inputs',invKind)}${metric('Reorder review point',readyInv()?Math.ceil(im.reorderPoint):'—','Demand × lead time + safety stock',invKind)}${metric('Cash headroom',readyCash()?money(cashx.headroom):'—','Vs your protected floor',cashKind)}${metric('Break-even orders',Number.isFinite(be)?Math.ceil(be):'—','Fixed launch cost ÷ post-CAC contribution')}</div>`:''}<div class="sectionbar"><h3>Next decision</h3><span>${sourceBadge()}</span></div><div class="actioncard"><b>${next[0]}</b><p>${next[1]}</p></div>${importBlock}`}
function profit(){const es=effectiveState(),c=readyProduct()?preCacContribution(es):null;return `${head('Profit & Pricing','Build one real order before trusting revenue.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('price','Selling price')}${field('landedCost','Landed product cost')}${field('packaging','Packaging')}${field('shippingSubsidy','Shipping subsidy')}${field('returnReserve','Returns / refund reserve')}${field('processorPercent','Processor %','','.01')}${field('processorFixed','Processor fixed fee')}</div></div><div class="result"><small>PRE-CAC CONTRIBUTION</small><div class="big">${c===null?'—':money(c)}</div>${row('Gross margin',readyProduct()?pct(grossMarginPct(es)):'—')}${row('Contribution margin',readyProduct()?pct(contributionMarginPct(es)):'—')}${row('Processor fee',readyProduct()?money(processorFee(es)):'—')}</div></div>${missingOrderCosts().length?`<div class="warning mt"><b>Incomplete cost stack.</b> Add ${missingOrderCosts().join(', ')} before treating contribution as complete.</div>`:''}`}
function cac(){const es=effectiveState(),mx=readyCacFloor()?maxFirstOrderCac(es):null,cac=effectiveCac(),pc=readyCac()?postCacContribution(es):null;return `${head('Customer Acquisition','Set the contribution you refuse to give up, then see the CAC ceiling it creates.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('requiredPostCac','Required post-CAC contribution')}${field('observedCac','Observed CAC','Leave blank to calculate from spend ÷ new customers.')}${field('adSpend','Ad spend')}${field('newCustomers','New customers','','1')}</div></div><div class="result"><small>CAC CEILING</small><div class="big">${mx===null?'—':money(mx)}</div>${row('Effective CAC',cac===null?'—':money(cac))}${row('Post-CAC contribution',pc===null?'—':money(pc))}</div></div>`}
function funnel(){const x=funnelMetrics(state);return `${head('Store Funnel','Use one consistent date range so conversion math is comparable.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('sessions','Sessions','','1')}${field('addToCarts','Add to carts','','1')}${field('checkouts','Reached checkout','','1')}${field('orders','Orders','','1')}</div></div><div class="result"><small>STORE CONVERSION</small><div class="big">${readyFunnel()?pct(x.conversionRate):'—'}</div>${row('Add-to-cart rate',hasInput('sessions')&&state.sessions>0?pct(x.addToCartRate):'—')}${row('Checkout rate',hasInput('sessions')&&state.sessions>0?pct(x.checkoutRate):'—')}</div></div>`}
function discount(){const es=effectiveState(),floor=readyCacFloor()?maxFirstOrderCac(es):null;return `${head('Discount Ceiling','Test offers against your contribution floor instead of copying another brand.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('discountPct','Discount %','','1')}${field('requiredPostCac','Required post-CAC contribution')}</div></div><div class="result"><small>REALIZED PRICE</small><div class="big">${readyProduct()&&hasInput('discountPct')?money(realizedPrice(es)):'—'}</div>${row('Pre-CAC contribution after offer',readyProduct()&&hasInput('discountPct')?money(preCacContribution({...es,price:realizedPrice(es)})):'—')}${row('Base max CAC',floor===null?'—':money(floor))}</div></div>`}
function inventory(){const x=inventoryMath(state);return `${head('Inventory & Reorder','Use actual sales pace and supplier lead time to create a reorder review point.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('weeklyDemand','Weekly demand')}${field('leadWeeks','Supplier lead time (weeks)')}${field('safetyWeeks','Safety stock (weeks)')}${field('onHand','On hand','','1')}${field('inbound','Inbound','','1')}${field('allocated','Allocated','','1')}</div></div><div class="result"><small>INVENTORY POSITION</small><div class="big">${readyInv()?Math.ceil(x.inventoryPosition):'—'}</div>${row('Reorder review point',readyInv()?Math.ceil(x.reorderPoint):'—')}${row('Weeks cover',readyInv()&&Number.isFinite(x.weeksCover)?x.weeksCover.toFixed(1):'—')}</div></div>`}
function wholesale(){const x=hasInput('wholesalePrice')?wholesaleContribution(state):null;return `${head('Wholesale Economics','Check what a wholesale order contributes before saying yes.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('wholesalePrice','Wholesale price')}${field('landedCost','Landed cost')}${field('wholesalePickPack','Wholesale pick / pack')}${field('wholesaleFreight','Freight per unit')}${field('wholesaleCommissionPct','Commission %','','1')}${field('retailPriceForRetailer','Suggested retail price')}</div></div><div class="result"><small>WHOLESALE CONTRIBUTION</small><div class="big">${x===null?'—':money(x)}</div>${row('Retailer gross margin',hasInput('wholesalePrice')&&hasInput('retailPriceForRetailer')?pct(retailerGrossMarginPct(state)):'—')}</div></div>`}
function launch(){const x=readyCac()&&hasInput('fixedLaunchCost')?breakEvenOrders(effectiveState()):Infinity;return `${head('Launch & Break-Even','Translate fixed launch cost into contribution-positive orders required to recover it.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('fixedLaunchCost','Fixed launch cost')}</div></div><div class="result"><small>BREAK-EVEN ORDERS</small><div class="big">${Number.isFinite(x)?Math.ceil(x):'—'}</div></div></div>`}
function fulfillment(){const n=hasInput('monthlyOrders')&&state.monthlyOrders>0?normalized3pl(state):Infinity,cross=crossoverOrders(state);return `${head('3PL Decision','Normalize monthly minimums and variable fees before comparing fulfillment options.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('monthlyOrders','Monthly orders','','1')}${field('inhouseFulfillment','In-house cost / order')}${field('thirdPartyVariable','3PL variable cost / order')}${field('thirdPartyMonthly','3PL monthly minimum / fixed fee')}</div></div><div class="result"><small>NORMALIZED 3PL COST</small><div class="big">${Number.isFinite(n)?money(n):'—'}</div>${row('In-house cost / order',hasInput('inhouseFulfillment')?money(state.inhouseFulfillment):'—')}${row('Cost crossover orders',Number.isFinite(cross)?Math.ceil(cross):'—')}</div></div>`}
function cash(){const x=readyCash()?cashCheckpoint(state):null;return `${head('Cash Checkpoint','Use one consistent time window and protect the operating cash floor you choose.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('cashStart','Starting cash')}${field('expectedInflows','Near-term inflows')}${field('wholesaleReceivable','Wholesale receivable expected in window')}${field('committedOutflows','Other committed outflows')}${field('supplierBalance','Supplier / PO balances already owed')}${field('payroll','Payroll')}${field('taxReserve','Tax reserve')}${field('processorHold','Processor hold')}${field('protectedFloor','Protected operating cash floor','Your own internal floor, not a universal benchmark.')}</div></div><div class="result"><small>PROJECTED CHECKPOINT</small><div class="big">${x?money(x.projectedCash):'—'}</div>${row('Headroom vs floor',x?money(x.headroom):'—')}</div></div><div class="advice mt"><b>Keep the window consistent.</b> Do not mix a two-week inflow estimate with a six-week outflow estimate and call the result runway.</div>`}
function po(){const x=readyCash()&&hasInput('proposedUnits')&&hasInput('depositPct')?purchaseOrderGate(state):null,pass=x&&x.minimumHeadroom>=0;return `${head('PO Cash Gate','Model the deposit today and the remaining supplier payment over time before approving inventory.','Your inputs')}<div class="grid g2"><div class="card"><div class="form">${field('proposedUnits','Proposed units','','1')}${field('landedCost','Landed cost / unit')}${field('depositPct','Deposit %','Supplier deposit due now.','1')}${field('poBalanceDueWeeks','Balance due in how many weeks?','Timing context for the remaining supplier payment.','.1')}${field('cashStart','Starting cash')}${field('expectedInflows','Near-term inflows')}${field('wholesaleReceivable','Wholesale receivable')}${field('committedOutflows','Other committed outflows')}${field('supplierBalance','Existing supplier balances')}${field('payroll','Payroll')}${field('taxReserve','Tax reserve')}${field('processorHold','Processor hold')}${field('protectedFloor','Protected cash floor')}${field('poExpectedInflowsBeforeBalance','Additional expected inflows before balance')}${field('poExpectedOutflowsBeforeBalance','Additional outflows before balance')}</div></div><div class="result"><small>PO DECISION</small><div class="big">${x?(pass?'PASS':'HOLD'):'—'}</div>${row('PO total',x?money(x.poTotal):'—')}${row('Deposit due now',x?money(x.deposit):'—')}${row('Cash after remaining balance',x?money(x.afterBalance):'—')}${row('Lowest headroom vs floor',x?money(x.minimumHeadroom):'—')}</div></div>`}
function shopify(){return `${head('Shopify CSV Dashboard','Import Shopify Orders or Transaction history CSV. Raw rows stay in your browser; Brand OS saves only aggregate summaries when signed in.','Shopify export-compatible')}<div class="drop"><input id="csvFile" type="file" accept=".csv,text/csv"><p><b>Choose Shopify CSV</b></p><p class="mini">Orders export: orders, exported order totals, units and SKU mix. Transaction history: successful captures/sales and refunds.</p></div>${lastImport?`${renderImport(lastImport)}<button id="clearImport" class="ghost mt">Clear imported snapshot</button>`:'<div class="empty mt"><b>No store data imported.</b><p>Brand OS will not show revenue, orders, AOV or refunds until you provide a source file.</p></div>'}`}
function renderImport(x,compact=false){if(x.kind==='transactions')return `<div class="grid g4 mt">${metric('Captured sales',money(x.capturedSales),'Successful sale/capture rows')}${metric('Refunds',money(x.refunds),'Successful refund rows')}${metric('Net captured payments',money(x.netCapturedPayments),'Captured minus refunds')}${metric('Orders with transactions',x.orderCountWithTransactions,'Unique order/name keys')}</div>`;return `<div class="grid g4 mt">${metric('Orders in export',x.orderCount,'Unique order names')}${metric('Non-canceled order total',money(x.nonCanceledOrderTotal ?? x.csvOrderTotal),'Order Total field')}${metric('Units in line items',x.units,'Line-item quantities')}${metric('Avg exported order total',money(x.averageOrderTotal),'CSV order total ÷ all exported orders')}</div>`}
function brandsView(){if(!session)return `${head('Brands & SKUs','Cloud saving needs an account. Calculators still work locally.','Secure cloud storage')}<div class="card"><p>Sign in to save brands and SKUs.</p><button class="primary" id="inlineSignIn">Sign in</button></div>`;const brandLimit=entitlement.limits?.brands,skuLimit=entitlement.limits?.skus,brandMaxed=!isPro()&&Number.isFinite(brandLimit)&&brands.length>=brandLimit,skuMaxed=!isPro()&&Number.isFinite(skuLimit)&&skus.length>=skuLimit;return `${head('Brands & SKUs','Save basic product economics under your account.','Owner-scoped cloud rows')}<div class="card"><div class="mini">${isPro()?'Pro: unlimited saved brands and SKUs.':`Free: ${brands.length}/${brandLimit??1} brand and ${skus.length}/${skuLimit??5} SKUs saved.`}</div>${brandMaxed?`<div class="advice mt"><b>Free brand limit reached.</b>Upgrade to Pro for unlimited brands and SKUs.</div>`:`<div class="split mt"><input id="brandName" placeholder="Brand name" style="flex:1;padding:11px;border:1px solid #ccc;border-radius:8px"><button id="addBrand" class="primary">Add brand</button></div>`}</div><div class="grid g2 mt">${brands.map(b=>`<div class="card"><div class="skuBar"><div><b>${escapeHtml(b.name)}</b><div class="mini">${escapeHtml(b.currency)}</div></div><button class="ghost" data-deletebrand="${b.id}" type="button">Delete brand</button></div><hr style="border:0;border-top:1px solid #eee"><div class="mini">${skus.filter(s=>s.brand_id===b.id).length} saved SKUs</div>${skuMaxed?`<div class="mini mt">Free SKU limit reached.</div>`:`<div class="split" style="margin-top:10px"><input id="sku-${b.id}" placeholder="SKU" style="width:90px;padding:8px"><input id="name-${b.id}" placeholder="Product" style="flex:1;padding:8px"><input id="price-${b.id}" type="number" min="0" step=".01" placeholder="Retail $" style="width:110px;padding:8px"><button data-addsku="${b.id}" class="outline">Add</button></div>`}${skus.filter(s=>s.brand_id===b.id).map(s=>`<div class="row"><span>${escapeHtml(s.sku)} · ${escapeHtml(s.name)}</span><b>${money(s.retail_price)}</b></div>`).join('')}</div>`).join('')||'<div class="card">No brands yet.</div>'}</div>`}
async function addBrand(){const input=$('#brandName'),btn=$('#addBrand'),name=input?.value.trim();if(!name){alert('Enter a brand name.');return}if(brands.some(b=>String(b.name||'').trim().toLowerCase()===name.toLowerCase())){alert('That brand already exists in your account.');return}if(btn?.disabled)return;if(btn){btn.disabled=true;btn.textContent='Saving…'}try{await api('/api/brands','POST',{name},true);await loadCloud();render()}catch(e){if(btn){btn.disabled=false;btn.textContent='Add brand'}alert(e.message)}}
async function deleteBrand(brandId){const brand=brands.find(b=>String(b.id)===String(brandId));if(!brand)return;const skuCount=skus.filter(s=>String(s.brand_id)===String(brandId)).length,warning=skuCount?`Delete ${brand.name} and its ${skuCount} saved SKU${skuCount===1?'':'s'}? This cannot be undone.`:`Delete ${brand.name}? This cannot be undone.`;if(!confirm(warning))return;try{await api(`/api/brands/${encodeURIComponent(brandId)}`,'DELETE',null,true);await loadCloud();await refreshAccount();render()}catch(e){alert(e.message)}}
async function addSku(brandId){const sku=$(`#sku-${brandId}`)?.value.trim(),name=$(`#name-${brandId}`)?.value.trim(),priceRaw=$(`#price-${brandId}`)?.value,retail_price=Number(priceRaw),btn=document.querySelector(`[data-addsku="${brandId}"]`);if(!sku||!name){alert('Enter both the SKU and product name.');return}if(priceRaw===''||!Number.isFinite(retail_price)||retail_price<0){alert('Enter a valid retail price.');return}if(skus.some(s=>String(s.brand_id)===String(brandId)&&String(s.sku||'').trim().toLowerCase()===sku.toLowerCase())){alert('That SKU already exists under this brand.');return}if(btn?.disabled)return;if(btn){btn.disabled=true;btn.textContent='Saving…'}try{await api('/api/skus','POST',{brand_id:brandId,sku,name,retail_price},true);await loadCloud();render()}catch(e){if(btn){btn.disabled=false;btn.textContent='Add'}alert(e.message)}}
function nextMoves(){const out=[];if(!readyProduct())return [['Set up product economics','Enter price and landed cost first. Add the remaining order-level costs before trusting contribution.']];const es=effectiveState(),pc=readyCac()?postCacContribution(es):null,im=inventoryMath(state),cashx=readyCash()?cashCheckpoint(state):null,poc=readyCash()&&hasInput('proposedUnits')&&hasInput('depositPct')?purchaseOrderGate(state):null,mx=readyCacFloor()?maxFirstOrderCac(es):null;if(!readyCac())out.push(['Finish the acquisition model','Set a post-CAC contribution floor and enter CAC directly or calculate it from spend ÷ new customers.']);else if(pc<=0)out.push(['Stop scaling acquisition','The modeled acquired order loses contribution. Fix price, variable cost, offer or CAC first.']);else if(effectiveCac()>mx+0.005)out.push(['Bring CAC back under the ceiling',`Modeled CAC ${money(effectiveCac())} is above the ${money(mx)} ceiling created by your own contribution floor.`]);const fullOperating=isPro()||mode==='demo'||mode==='demo-edited';if(fullOperating&&readyCash()&&cashx.headroom<0)out.push(['Protect cash',`${money(Math.abs(cashx.headroom))} below the protected floor in the quick checkpoint.`]);if(fullOperating&&poc&&poc.minimumHeadroom<0)out.push(['Hold the proposed PO',`The modeled payment timeline falls ${money(Math.abs(poc.minimumHeadroom))} below the protected cash floor at its lowest point.`]);if(fullOperating&&readyInv()&&im.inventoryPosition<=im.reorderPoint)out.push(['Review the next reorder',`${Math.ceil(im.inventoryPosition)} units positioned vs ${Math.ceil(im.reorderPoint)} review point. Confirm demand quality and cash before ordering.`]);if(readyCac()&&pc>0&&effectiveCac()<=mx+0.005&&out.length===0)out.push(['Protect the economics',`Modeled first-order contribution after acquisition is ${money(pc)}.`]);return out}
function advisor(){const out=nextMoves();return `${head('Next Move Advisor','Rule-based operating prompts from your inputs. These are not forecasts and they do not replace judgment.','Motion Supply operating logic')}<div class="grid g2">${out.map((x,i)=>`<div class="card"><span class="kicker">MOVE ${i+1}</span><h3>${x[0]}</h3><p>${x[1]}</p></div>`).join('')}</div>`}
function escapeHtml(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt',"'":'&#39;','"':'&quot;'}[c]))}
function render(){const title=$('#title'),app=$('#app');if(title)title.textContent=views[current];if(!app)return;const map={dashboard,profit,cac,funnel,discount,inventory,wholesale,launch,fulfillment,cash,po,shopify,brands:brandsView,advisor};app.innerHTML=isProLocked(current)?proGate(current):map[current]();bind();updateAccount()}
function bind(){document.querySelectorAll('[data-key]').forEach(el=>el.onchange=e=>{const key=e.target.dataset.key,raw=e.target.value;if(raw===''){touched.delete(key);state[key]=0}else{touched.add(key);state[key]=Number(raw)}mode=mode==='demo'||mode==='demo-edited'?'demo-edited':'fresh';save();if(readyProduct())trackOnce('first_calculation_completed');render()});document.querySelectorAll('[data-jump]').forEach(el=>el.onclick=()=>jumpTo(el.dataset.jump));const f=$('#csvFile');if(f)f.onchange=handleCsv;const c=$('#clearImport');if(c)c.onclick=()=>{lastImport=null;save();render()};const s=$('#inlineSignIn');if(s)s.onclick=showAuth;document.querySelectorAll('[data-deletebrand]').forEach(el=>el.onclick=()=>deleteBrand(el.dataset.deletebrand));const ab=$('#addBrand');if(ab)ab.onclick=addBrand;const up=$('#upgradeNow');if(up)up.onclick=upgradeFlow;document.querySelectorAll('[data-addsku]').forEach(b=>b.onclick=()=>addSku(b.dataset.addsku))}
function closeMenu(){const side=$('#side'),btn=$('#menuBtn');side?.classList.remove('open');document.body.classList.remove('menuOpen');btn?.setAttribute('aria-expanded','false')}
function openMenu(){const side=$('#side'),btn=$('#menuBtn');side?.classList.add('open');document.body.classList.add('menuOpen');btn?.setAttribute('aria-expanded','true')}
function toggleMenu(){if($('#side')?.classList.contains('open'))closeMenu();else openMenu()}
function jumpTo(view){track('tool_opened',{view});current=view;document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view===view));closeMenu();render();scrollTo(0,0)}
async function handleCsv(e){const file=e.target.files[0];if(!file)return;if(session&&!entitlement.active&&config.billingConfigured){alert('Shopify CSV dashboard is a Pro feature for signed-in users.');return}try{const parsed=summarizeShopifyCsv(await file.text()),clean={...parsed};delete clean.orders;lastImport=clean;save();render();if(session)await api('/api/import-summaries','POST',{file_kind:lastImport.kind,start_date:lastImport.startDate||null,end_date:lastImport.endDate||null,summary:lastImport},true)}catch(err){alert(err.message)}}
function showAuth(){if(!config.authConfigured)return alert('Account service is not fully configured on this deployment yet.');if(session)return showAccount();showModal(`<h2>Sign in / create account</h2><div class="authform"><input id="email" type="email" placeholder="Email"><input id="password" type="password" placeholder="Password (8+ characters)"><div id="authMsg" class="mini"></div><div class="split"><button id="signin" class="primary">Sign in</button><button id="signup" class="outline">Create account</button></div></div>`);$('#signin').onclick=()=>auth('signin');$('#signup').onclick=()=>auth('signup')}
async function auth(kind){if(kind==='signup')track('signup_started');const email=$('#email').value.trim(),password=$('#password').value,msg=$('#authMsg');msg.textContent='Working…';try{const x=await api(`/api/auth/${kind}`,'POST',{email,password});if(x.confirmationRequired){msg.textContent='Account created. Check your email to confirm, then sign in.';return}storeSession(x.session);if(kind==='signup')track('signup_completed');hideModal();await refreshAccount();render()}catch(e){msg.textContent=e.message}}
function showAccount(){showModal(`<h2>Account</h2><p>${escapeHtml(session?.user?.email||'Signed in')}</p><p>Plan: <b>${entitlement.plan.toUpperCase()}</b></p><div class="split"><button id="signout" class="outline">Sign out</button>${config.billingConfigured?`<button id="acctBilling" class="primary">${(entitlement.active||entitlement.billingManageable)?'Manage billing':'Upgrade to Pro'}</button>`:''}</div>`);$('#signout').onclick=()=>{storeSession(null);brands=[];skus=[];hideModal();refreshAccount();render()};const b=$('#acctBilling');if(b)b.onclick=startBilling}
async function startBilling(){try{const manage=Boolean(entitlement.active||entitlement.billingManageable);if(!manage)track('checkout_started');const endpoint=manage?'/api/create-portal-session':'/api/create-checkout-session',x=await api(endpoint,'POST',{},true);location.href=x.url}catch(e){alert(e.message)}}
function showModal(html){$('#modalBody').innerHTML=html;$('#modal').classList.remove('hidden')}function hideModal(){$('#modal').classList.add('hidden')}
function loadFoundryDemo(){state=clone(FOUNDRY_EIGHT);mode='demo';touched=new Set(Object.keys(FOUNDRY_EIGHT));lastImport=null;current='dashboard';save();render();document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view==='dashboard'));closeMenu();window.scrollTo(0,0)}
function startFreshModel(){state=clone(STARTER_STATE);mode='fresh';touched=new Set();lastImport=null;current='dashboard';save();render();document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view==='dashboard'));closeMenu();window.scrollTo(0,0);const btn=$('#freshBtn');if(btn){const old=btn.textContent;btn.textContent='Fresh model loaded ✓';setTimeout(()=>{if(btn.isConnected)btn.textContent=old},1200)}}
document.addEventListener('click',e=>{const navBtn=e.target.closest('#nav button[data-view]');if(navBtn){jumpTo(navBtn.dataset.view);return}if(e.target.closest('#menuBtn')){toggleMenu();return}if(e.target.closest('#menuCloseBtn')||e.target.closest('#menuBackdrop')){closeMenu();return}if(e.target.closest('#demoBtn')){e.preventDefault();loadFoundryDemo();return}if(e.target.closest('#freshBtn')){e.preventDefault();startFreshModel();return}if(e.target.closest('#authBtn')){showAuth();return}if(e.target.closest('#billingBtn')){startBilling();return}if(e.target.closest('#closeModal')){hideModal();return}if(e.target.id==='modal'){hideModal();return}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(!$('#modal')?.classList.contains('hidden'))hideModal();closeMenu()}});
init();
